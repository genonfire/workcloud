// This is a sample file for config.json.
// Copy content into config.json for the project to load.
{
    "FRONTEND_THEME": "wc",
    "REST_PAGINATION_SIZE_DEFAULT": 20,
    "DJANGO_DEBUG": true,
    "LOCAL_SERVER": true
}
